{% extends 'base.html.twig' %}

{% block title %}
{{ wish.title }} | {{ parent() }}
{% endblock %}

{% block body %}
    <h1>{{wish.title}}</h1>

    {# affiche tous les détails de l'idée #}
    <div>{{ wish.description|nl2br }}</div>
    <div>Wish created by {{ wish.author }} on {{ wish.dateCreated|date("Y-m-d") }}</div>
{% endblock %}

